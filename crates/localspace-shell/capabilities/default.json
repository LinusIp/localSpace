{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "The main window. The web client talks to Core over loopback HTTP and WebSocket, not over Tauri IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
