// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CapabilitySummary } from "./CapabilitySummary";
import type { DocKind } from "./DocKind";
import type { ResourceSummary } from "./ResourceSummary";
import type { Tier } from "./Tier";

/**
 * One package on the store page.
 */
export type CatalogEntry = { id: string, title: string, version: string, publisher: string, description: string, tier: Tier, 
/**
 * Shown verbatim, as the spec requires, whenever `tier = native`.
 */
native_reason: string | null, tool_count: number, front_door: Array<string>, capabilities: CapabilitySummary, 
/**
 * Capabilities in plain language, for a reader who is not an engineer.
 */
capability_lines: Array<string>, doc_kind: DocKind, has_context_provider: boolean, eval_cases: number, 
/**
 * Where this package came from: a bundle directory, or a registry.
 */
source: string, path: string, installed: boolean, installed_version: string | null, 
/**
 * Non-empty when installing this would widen what the harness may do.
 * An update that widens capabilities does not auto-install.
 */
widens: Array<string>, 
/**
 * Set when the package cannot be installed here, and why.
 */
blocked: string | null, resources: ResourceSummary, 
/**
 * `harness`, `library`, `types`, `template`, `model-pack`, `skill`, `theme`.
 */
kind: string, 
/**
 * Dependencies as declared, e.g. `io.localspace.types.geometry ^1.2`.
 */
dependencies: Array<string>, };
